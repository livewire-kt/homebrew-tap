# Livewire Homebrew Tap

Homebrew cask for the [Livewire](https://github.com/livewire-kt/livewire) desktop
app (macOS).

## Install

```bash
brew install --cask livewire-kt/tap/livewire
```

or

```bash
brew tap livewire-kt/tap
brew install --cask livewire
```
